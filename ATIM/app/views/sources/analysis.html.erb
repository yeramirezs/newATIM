<%= render "layouts/header"%>
</br>
</br>
</br>
</br>
<table width="100%"  style="left:0px;">
  <tr>
    <td width="20%" valign="top" align="center">
      <table  style="background-color:#E6E6E6; border-radius: 15px;" width="70%">
        <tr>
          <td>
            <ul class="nav nav-list">
              <li class="nav-header">Options</li>
              <li class="active"><%= link_to "Home", index_path(:email=>@teacher.email), :post => true%></li>
              <li><%= link_to "Crear Tesis", new_thesis_path(:id=>@teacher.id) %></li>
            </ul>
          </td>
        <tr><td>&nbsp;</td></tr>
        <tr><td>&nbsp;</td></tr>
        </tr>
      </table>
    </td>
    <td width="75%">
      <%= stylesheet_link_tag 'sources.css', :media => 'screen' %>
      <h2 class="center">Fuente de investigación <%=@source.title%> de la tesis</h2>  <br/>
      <h2 class="center"> <%= @thesis_id %> -  <%=  @thesis.title %>  </h2>
      <br/>

      <table id="sources" class="tabla">
        <tr>
          <th> Analysis </th>
        </tr>
        <tr>
          <td class="text">
            <%= form_for @source, :url => {:controller=>"sources", :action => "update", :method=> :put }, :html => {:class => "table"} do |f| %>
              <div>
                <%= hidden_field_tag(:id, @source.id) %>
                <%= text_area_tag('analysis', @source.analysis, :class=>"text", :rows => 15, :cols => 45) %>
              </div>
              <div class="actions">
                <%= f.submit  "Update analysis", :class => "btn" %>
              </div>
            <% end %>
          </td>

        </tr>
      </table>
      <div class="link">
        <br> <br>
        <%= link_to 'Regresar', thesis_path(:id =>@thesis.id, :id2 =>@teacher.id), class: "btn btn-large btn-primary"%>
      </div>
    </td>
  </tr>
</table>

