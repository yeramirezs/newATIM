<%= form_for(@teacher) do |f| %>
  <% if @teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacher.errors.count, "error") %> prohibited this teacher from being saved:</h2>

      <ul>
      <% @teacher.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <table>
	<tr><td>&nbsp;</td></tr>
	<tr><td>&nbsp;</td></tr>	
  </table>
	<table class="form-signin">
	      <tr>
	          <td><h1 class="form-signin-heading">New Teacher</h1></td>
	      </tr>
	      <tr>
	          <td> <%= f.text_field :name, :placeholder => 'Complete name' %></td>
	      </tr>
	      <tr>
	          <td> <%= f.text_field :email, :placeholder => 'Email'%></td>
	      </tr>
	      <tr><td>&nbsp;</td></tr>
	      <tr><td><%= f.submit "Sign up",  class: "btn"%> </tr></td>
	      <tr><td><%= link_to 'Atras', root_path%> </tr></td>
		</td></tr>
	</table>
<% end %>


